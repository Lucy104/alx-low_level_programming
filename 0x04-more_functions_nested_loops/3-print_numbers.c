#include <stdio.h>
#include "main.h"

/**
  * print_numbers - check the code
  * @l: parameter
  * Return: 0 (Success)
  */
void print_numbers(void)
{
	int l;

	for (l = 48; l < 58; l++)
	{
		_putchar(l);
	}

	_putchar('\n');
}

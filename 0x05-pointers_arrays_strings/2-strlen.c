#include <stdio.h>
#include "main.h"
#include <string.h>

/**
  * _strlen - function that returns the length of a string.
  * @s: parameter
  * Return: void
  */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}

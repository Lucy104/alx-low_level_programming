#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  * *_strcat - function that concatenates two strings
  * @*dest: first string
  * @*src: second string
  * Return: 
  */
char *_strcat(char *dest, char *src)
{
	return strcat(dest, src);
}
